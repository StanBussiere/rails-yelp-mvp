<h1>All Restaurants</h1>
<ul>
  <% @restaurants.each do |restaurant| %>

  <li>
    <p><%= link_to restaurant.name, restaurant  %>, <%= restaurant.address %>
    <span>(<%= restaurant.category %>)</span></p>
  </li>
  <% end %>
  <p><%= link_to 'Add a restaurant', new_restaurant_path %></p>
</ul>
